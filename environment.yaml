# conda env create -f environment.yaml
name: py38
channels:
  - conda-forge  #defaults
dependencies:
  - python=3.8
  - pip
  - numpy
  - pandas
  - scikit-learn
  - scipy
  - pyyaml
  - matplotlib
  - pip:
    - joblib